#---- bin

INCLUDE(FadaSummary)
LINK_DIRECTORIES(${Fada_LIBRARY_INSTALL_DIR} ${EXTERNAL_LIBRARY_DIR} )


SET(EXECUTABLES
Crop
EstimatorToMarkedCells
FadaMeshToSimpleMesh
MarkedCellsTriangleMeshToQuadMesh
MeshCoarsener
MeshRefiner
MultiMeshConstructor
MultiMeshConstructorQuadToTri
ReaderMesh
Rotate
)


FOREACH(EXECUTABLE ${EXECUTABLES})
  AUX_SOURCE_DIRECTORY(${EXECUTABLE}  SRC${EXECUTABLE})
  ADD_EXECUTABLE(${EXECUTABLE} ${SRC${EXECUTABLE}})
  TARGET_LINK_LIBRARIES(${EXECUTABLE} fadalib ${Fada_EXTERNAL_LIBRARIES} )
  INSTALL(TARGETS ${EXECUTABLE} DESTINATION ${CMAKE_INSTALL_PREFIX}/bin)
ENDFOREACH(EXECUTABLE)
